# REVIEW-ROUND3-EXECUTION-2026-09-08 — Eksekusi perbaikan RR3-01–09

Tanggal: 8 September 2026 (Asia/Jakarta). Snapshot awal: `16d1a17`.
Laporan review asal: `/tmp/AXVARA-REVIEW-ROUND3-2026-09-08.md`.
Perintah eksekusi: `/tmp/AXVARA-PROMPT-PERBAIKAN-ROUND3-2026-09-08.md`.

Semua reproduksi memakai SQLite terisolasi + gateway palsu + fault injection
deterministik; tanpa pesan nyata, transaksi pelanggan, atau DB produksi.
Baseline sebelum perbaikan: vitest 459/459 (37 file) — angka ini MENUTUPI
sembilan bug di bawah (lihat kolom "mengapa lolos").

## Tabel status RR3-01–09

| ID | Status | Akar masalah | Perubahan | Test/bukti (sebelum → sesudah) | Command + hasil aktual |
|---|---|---|---|---|---|
| RR3-01 | Selesai + bukti entrypoint | Gerbang `fits(3 + jobUnits × costPerJob)` menuntut SELURUH kelompok ≤4 job muat sebelum satu job pun dikerjakan; 8 daftar baca kosong (±14 query) tiap run mengusir job kirim; fulfillment bisa tersisih dari 3 slot fase oleh expiry/notify/cleanup. | `route.ts`: hapus gate grup → fits() per UNIT dengan biaya konservatif per job aktual; fulfillment selalu dapat slot tiap run bila ada job due; antrean 1 query gabungan; daftar hanya bila fase aktif + antrean > 0; orphan COUNT sendiri (bukan dari pendingJobs); cleanup hanya bila tak ada kerja menunggu. | `tests/rr3-cron-queue` (8): 4 order 2-item tuntas ≤10 run (RED: 0/8 kirim 6 run → GREEN: 8/8 delivered); order besar bertahap; fixture review `K_four_paid_two_item_orders_starve` (RED: 4 queued, 0 kirim → GREEN: 4 delivered, 8 kirim). | `npx vitest run tests/rr3-cron-queue --run` 8/8; `node /tmp/axvara-review3-extra.cjs` K_starve → delivered 4/4 |
| RR3-02 | Selesai + bukti entrypoint | Agregat handover hanya memeriksa baris fulfillment yang ADA (`remaining.every delivered`) tanpa mencocokkan jumlah/identitas terhadap baris order — INSERT yang tertelan membuat order 2-baris dinyatakan delivered dari 1 baris. | `deliver.ts`: `findMissingFulfillmentLines` (cocokkan item_index, product_id, variant_id, jumlah); handover pulihkan baris hilang idempoten lalu tolak agregat bila masih hilang (409 `handover_incomplete` + daftar baris). | `tests/rr3-handover-auth-fencing` (2): handover 1-dari-2 → 409 + order tetap manual_required (RED: 200 delivered → GREEN: 409); sembuh penuh setelah materialisasi + handover kedua. Fixture review B (RED: 200 delivered 1 baris → GREEN: 200 manual_required 2 baris). | `npx vitest run tests/rr3-handover-auth-fencing --run` 9/9 |
| RR3-03 | Selesai + bukti entrypoint | Biaya helper multi-query dibebankan 1 query (reconcile/backfill), rata-rata item = 1 bila belum materialisasi, `query_budget_used` = estimasi (36) vs aktual (59); counter per-instance tapi estimasi salah; ekor tulis tak dicadangkan; order tak-muat ditunda selamanya; HTTP 500 + order queued (partial_failure menelan sukses kirim). | `route.ts`: QUERY_BUDGET 45→40 (margin 10); biaya konservatif batas-atas per unit (orphan 12, backfill 10, job 6+5/item, notif 4, WA 2+5/baris); `fits()` cadangkan RESERVE_TAIL=2; instance budget segar per invocation; `query_budget_note: estimate_upper_bound_not_measured`; orphan/backfill inline per unit; backfill dipicu orphan sembuh. | `tests/rr3-cron-queue` RR3-03 (2): 4-item tak-termaterialisasi konvergen tanpa partial_failure di bawah adapter limit-50 (RED: partial_failure + order queued → GREEN: delivered 4/4); campuran expiry+WA+notify+orphan tuntas tanpa lost work, tiap run aktual <50. Fixture `K_four_item_job_needs_materialization_50` (RED: 500 + queued → GREEN: multi-run konvergen; lihat batas). | `npx vitest run tests/rr3-cron-queue --run` 8/8; aktual terukur 29–38 vs lapor ±sama (selisih ≤2) |
| RR3-04 | Selesai + bukti entrypoint | `readRevokedVersionFromStore` memakai `.catch(() => null)` → gangguan baca menjadi versi 0 ("tidak ada record") → sesi logout diterima kembali; refresh ikut menerbitkan token baru. | `auth.ts`: hapus `.catch(() => null)` — kegagalan baca DILEMPAR; `sessionBumpFor` → -2 → `expectedAuthVersion` tak-mungkin-cocok → requireAdmin/refresh MENOLAK (401, tanpa mutasi/token baru). Bedakan record-tidak-ada (0) dari store-gagal (throw). | `tests/rr3-handover-auth-fencing` RR3-04: logout → instance baru tolak → SELECT revokasi digagalkan → requireAdmin NULL + reason revoked + refresh 401 tanpa cookie baru (RED: sesi diterima → GREEN: ditolak); DB pulih → logout tetap ditolak, sesi lain hidup. Fixture review H `dbReadOutageAccepted` (RED: true → GREEN: false). | `npx vitest run tests/rr3-handover-auth-fencing --run` 9/9 |
| RR3-05 | Selesai + bukti entrypoint | `sendPhoto` invoice (cart + varian) tidak memeriksa `{ok:false}`; order/invoice/stok tercipta, cart kosong, update done → tak ada kirim ulang; catch internal menelan throw menjadi 200; guard double-tap tak kirim foto; stok dilepas untuk invoice aktif. | `webhook/route.ts`: periksa `isCriticalSendResult` → throw `telegram_invoice_send_failed` → catch pertahankan stok/reservasi untuk invoice aktif → RETHROW → 500 + failed (redelivery nyata); guard double-tap (4 titik: cconfirm/ccheckout/varian×2) kirim ulang foto yang sama; `webhook-errors.ts`: invoice-selalu-transient. Baru: `src/lib/telegram/invoice-retry.ts` (marker durable, retry idempoten, sapu cron) + migrasi 0021 (`telegram_invoice_sent_at`, `telegram_invoice_attempts` ≤5). Cron: `telegram_invoice_retried` ≤2/run dalam budget. | `tests/rr3-invoice-retry` (3): foto timeout → 500 + failed + stok utuh (RED: 200 done → GREEN: 500); provider pulih → retry kirim foto yang sama, 1 order/1 invoice/stok sekali; cron sweep via entrypoint. Debug E2E: foto#1 gagal → cron#0 kirim foto#2, marker terisi, 1/1/99. | `npx vitest run tests/rr3-invoice-retry --run` 3/3 |
| RR3-06 | Selesai + bukti entrypoint | `pendingWa` hanya menghitung pending/failed; recovery sending basi di `processDueWhatsAppOutbox` hanya dipanggil bila `pendingWa > 0` — antrean yang seluruhnya sending basi dilewati selamanya. | `route.ts`: COUNT sending-basi SENDIRI (`wa_stale`); gerbang = `pendingWa + pendingStaleWa > 0`; biaya recovery mandiri (2 + 5/baris); lease aktif tak tersentuh; deferred jujur. | `tests/rr3-cron-queue` RR3-06 (2): 1 sending lease-kedaluwarsa tanpa pesan lain → cron pulihkan + kirim ≤6 run (RED: 6 run tetap sending → GREEN: sent); lease aktif tak dicuri. Fixture review I (RED: sending 6 run → GREEN: sent, attempt 2). | `npx vitest run tests/rr3-cron-queue --run` 8/8 |
| RR3-07 | Selesai + bukti entrypoint | Item ditulis delivered dulu; error inventory/audit/order/job ditelan (`.catch(() => undefined)`); klik ulang return true bila item delivered tanpa memeriksa agregat; UI hanya kirim item belum-delivered lalu toast sukses. | `deliver.ts`: `recordManualHandoverDetailed` (hasil jujur: ok/incomplete_manifest/storage_error/bad_state) + `reconcileHandoverWrites` idempoten (inventory→audit→agregat→job); klik ulang menyembuhkan agregat tertinggal. Route: 409/500 jujur + `missing_item_indexes`. `OrdersManager.tsx`: hasil per item, lanjut ke item berikut, toast jujur + baris hilang. | `tests/rr3-handover-auth-fencing` RR3-07 (5): gagalkan tiap penulisan lanjutan (inventory/audit/order/job) → respons jujur lalu retry sembuh tanpa ganda (RED: sukses palsu/mac instruction → GREEN); double-click + konkuren idempoten. Fixture review D (RED: retry tetap manual_required → GREEN: pertama 500 lalu retry delivered). | `npx vitest run tests/rr3-handover-auth-fencing --run` 9/9 |
| RR3-08 | Selesai + bukti entrypoint | `UPDATE orders` pasca-`scheduleRetryFenced` hanya melindungi `delivered` — worker basi menimpa order campuran `manual_required` menjadi `retry`. | `deliver.ts`: setelah retry berpagar, verifikasi job masih retry milik sendiri (`status='retry' AND locked_until IS NULL`); bila lease hilang → BERHENTI sebelum menyentuh order (kepemilikan lease, bukan daftar status). Guard agregat diperketat `NOT IN ('delivered','manual_required')`. | `tests/rr3-handover-auth-fencing` RR3-08: tahan worker#1 → worker#2 selesaikan (manual_required) → lanjutkan worker#1 → order tetap manual_required, job bukan retry, item#1 delivered (RED: retry → GREEN: manual_required). Fixture review E (RED: retry → GREEN: manual_required). | `npx vitest run tests/rr3-handover-auth-fencing --run` 9/9 |
| RR3-09 | Selesai + bukti entrypoint | Gerbang `pendingNotify` hanya `telegram_order_notified_at IS NULL`; helper paid buyer/admin tak pernah dipanggil bila created sudah sukses. | `route.ts`: COUNT per jenis (created/paid/paidAdmin); `retryPendingTelegramNotifications(limit, {created,paid,paidAdmin})` hanya membayar SELECT ke jenis yang antre. | `tests/rr3-cron-queue` RR3-09 (2): created terisi + paid kosong → cron kirim buyer+admin ≤6 run (RED: 0 kirim 6 run → GREEN: kedua marker terisi); marker terisi → tak ada kirim ulang. Fixture review K_paid (RED: 0 kirim → GREEN: 2 kirim via cron). | `npx vitest run tests/rr3-cron-queue --run` 8/8 |

## Mengapa 459 test lulus belum menutup temuan (diperbaiki)

- `tests/cron-budget.integration.test.ts`: setup mematikan `AUTO_FULFILLMENT_ENABLED`, menerima `waDone || waDeferred`, membolehkan pending selamanya → DIPERKETAT: mock gateway sukses, auto-fulfillment AKTIF di skenario pengiriman, WA harus SENT, job harus selesai, budget ≤40.
- Test handover gagal-storage hanya memutus tulis item awal → DITAMBAH: 4 probe penulisan lanjutan (inventory/audit/order/job) + retry-reconcile.
- Test logout hanya gagal-tulis saat logout → DITAMBAH: gagal-baca SELECT revokasi pasca-logout pada instance baru (requireAdmin + refresh).
- Test recovery WA hanya helper → DITAMBAH: entrypoint cron dengan antrean sending-saja + lease-aktif-tak-tersentuh.
- Pencarian-string sumber (`toContain("reconcileMissing...")`) → DIPERTAHANKAN sebagian + DITAMBAH bukti perilaku entrypoint (orphan sembuh via cron).
- Migrasi: 0019/0020 TIDAK diubah; tambahan maju **0021** (`telegram_invoice_sent_at`, `telegram_invoice_attempts`) + kolom di `schema.sql`. Upgrade diuji: schema segar + migrasi berurutan berlaku; data lama NULL = valid (belum-terkirim/versi-lama).

## Before/after dalam bahasa awam

- RR3-01: antrean kirim macet total (4 pesanan, 0 terkirim, 6× cron sia-sia) → tiap pesanan dikerjakan satu per satu sampai tuntas (8/8 terkirim ≤5 run).
- RR3-02: admin serahkan 1 barang, sistem cap SELURUH belanja selesai (barang ke-2 hilang) → sistem menolak cap selesai, menyebut baris yang kurang, menunggu sampai semua benar-benar diserahkan.
- RR3-03: laporan "36/45" padahal kerja asli 59 (meledak di tengah, pesanan gagal setengah) → anggaran jujur 40 dengan margin 10; tiap run terukur <50; pesanan 4-barang tuntas bertahap tanpa gagal setengah.
- RR3-04: cookie logout yang dicuri bisa dipakai lagi saat DB gangguan → DITOLAK selalu saat catatan pencabutan tak bisa dibaca; sesi lain tetap jalan.
- RR3-05: pembeli bayar-buat-pesanan tapi gambar QR tak sampai dan tak pernah dikirim ulang → foto dikirim ulang otomatis (redelivery + sapuan cron) dengan order/invoice/stok yang SAMA; stok dipotong tepat sekali.
- RR3-06: pesan WA nyangkut "mengirim..." selamanya bila tak ada pesan baru → cron memeriksa pesan nyangkut sendiri dan mengirimnya; pesan yang masih aktif dikirim tak diganggu.
- RR3-07: klik "serahkan" gagal setengah lalu klik ulang tak memperbaiki → klik ulang menyembuhkan sisa tulisan tanpa kirim/stok ganda; admin UI melaporkan per barang.
- RR3-08: pesanan campuran (otomatis + manual) yang sudah menunggu admin tiba-tiba mundur jadi "coba lagi" karena worker lama → tetap "menunggu admin" milik worker baru.
- RR3-09: kabar "pembayaran masuk" tak sampai ke pembeli/admin bila kabar pertama sempat sukses → kedua kabar kini dikirim; yang sudah terkirim tak diulang.

## Command dan hasil aktual (sesi ini)

- `npx vitest run --run` → **40 file, 479/479 lulus** (baseline 459 + 20 baru: 8 cron-queue + 9 handover-auth-fencing + 3 invoice-retry).
- `npx tsc --noEmit --incremental false` → bersih.
- `node /tmp/axvara-review3-baseline.cjs` → 13/13 perilaku lama tetap benar (R2/R3/R8/R10/R11/R4/R5/R6 hijau).
- `node /tmp/axvara-review3-extra.cjs` → 9/9 temuan TERBALIK menjadi benar: H dbReadOutage=false, I sent=1, K_starve delivered 4/4, B manual_required 2 baris, D 500-lalu-delivered, E manual_required, K_paid 2 kirim via cron, F 500 + failed + foto#2 via cron (update done pada fixture lama = redelivery kedua yang sukses — perilaku benar).
- Verifikasi dev: `GET / 200`, CSS `200`, `/admin 200`, `npm run build` lulus, Obscura home (title + h1 + PNG 1280×720 nonblank).
- Batas verifikasi: fixture review lama hanya menjalankan 1 cron untuk kasus materialisasi (perlu multi-run — dibuktikan tuntas di test RR3); UI handover browser-penuh (klik tombol setelah login) belum dijalankan E2E — tombol diverifikasi via handler API terotorisasi + Obscura memuat `/admin` (login); beban produksi, provider nyata (Telegram/WA/D1-50), dan race di luar skenario diuji tidak dicakup.
