import {NextRequest,NextResponse}from"next/server";import{queryAll}from"@/lib/db";import{requireAgent}from"@/lib/agent-auth";export const runtime="edge";export async function GET(req:NextRequest){const a=await requireAgent(req,"audit:read");if("error" in a)return NextResponse.json({error:a.error},{status:a.status});return NextResponse.json({activity:await queryAll("SELECT * FROM article_audit_log ORDER BY created_at DESC LIMIT 100")},{headers:{"Cache-Control":"private, no-store"}});}
